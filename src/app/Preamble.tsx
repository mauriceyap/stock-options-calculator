import { Box, Typography } from "@mui/material";

export const Preamble = () => (
  <Box>
    {/* eslint-disable-next-line @cspell/spellchecker */}
    <Typography paragraph>weclome</Typography>
  </Box>
);
